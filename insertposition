int searchinsert(int* num, int numSize, int target) {
    int i, res;
    for (i = 0; i < numsSize; i++) {
        if (target == num[i] || num[i] > target) {
            return i;
            break;
        } else {
            res = i + 1;
        }
    }
    return res;
}
